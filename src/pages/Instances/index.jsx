import React from 'react';
import styles from './../../styles/styles.module.scss';

const Instances = () => {
  return <div className={styles.container}>Instances</div>;
};

export default Instances;
